

## Updated Plan: Rebuild Martin Dobiš Insurance Website in React

Everything from the previous plan remains unchanged, plus one addition:

### New Addition: Custom "MD" Favicon

1. **Create a simple "MD" favicon** — A minimal, clean favicon showing the letters "MD" (matching the site's navy/gold brand colors), generated as an SVG favicon for crisp rendering at all sizes

2. **Apply it in index.html** — Update the `<link rel="icon">` tag so the MD icon appears in browser tabs and as the site icon in Google search results (replacing the default globe)

### Preserved from Previous Plan

3. **Convert the full HTML page to React** — Recreate the entire single-page website with all sections, styling, animations, colors (navy/gold), fonts, and responsive behavior

4. **Replace the profile photo** — Use the new uploaded portrait image

5. **Update all CTA/form links** — Point everything to `https://tally.so/r/BzGLP7`

6. **Preserve SEO** — All meta tags, Open Graph tags, structured data (JSON-LD), and semantic HTML

7. **Preserve all sections & animations** — Navigation, Hero, Services, Process, FAQ, Contact, Footer with all scroll animations

